import { Typography } from "@mui/material";

export default function StatsPage() {
  return (
    <Typography variant="h5">
      Statistics page – implement click tracking & analytics here.
    </Typography>
  );
}